# ── Google OAuth ──────────────────────────────────────────────────────────────
# Create a project at https://console.cloud.google.com/
# Enable "Google Identity Services" and create an OAuth 2.0 Client ID (Web application)
# Add http://localhost:3001 to "Authorised JavaScript origins"
# Add http://localhost:3000/auth/lastfm/callback to Last.fm app callback (separate)
REACT_APP_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# Optional: backend API base URL
REACT_APP_BACKEND_URL=http://localhost:3000
